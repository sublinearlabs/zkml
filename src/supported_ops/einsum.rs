#[derive(Debug, Clone)]
pub(crate) struct Einsum {
    name: String,
    id: usize,
    instruction: String,
    input_count: usize,
    output_count: usize,
}

impl Einsum {
    fn create_circuit() {
        todo!()
    }
}
